# PY_Test_Repo_001
Repo for Python projects and testing
This repo currently has a drivers.py file that generates a requested amount of random names.

Drivers is my progression through creating the complex version to the script only allowing the three depts
Each dept has a store of acceptable inputs that will all return the uniform 3 letter dept name